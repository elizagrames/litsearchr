get_condensed_terms(
  reduced_graph = reduce_graph(BBWO_graph, cutoff_strength = 15),
  previous_graphs = list(reduce_graph(BBWO_graph,
                                      cutoff_strength = 20))
)
